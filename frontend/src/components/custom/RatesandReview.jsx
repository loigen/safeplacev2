import React from "react";

const RatesandReview = ({ setView }) => {
  return (
    <div>
      <p
        onClick={() => setView("settings")}
        className="text-black"
        style={{ cursor: "pointer", marginBottom: "1rem" }}
      >
        Back
      </p>
      ;RatesandReview
    </div>
  );
};

export default RatesandReview;
